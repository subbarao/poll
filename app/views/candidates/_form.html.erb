<%= form_for(@candidate) do |f| %>
  <% if @candidate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@candidate.errors.count, "error") %> prohibited this candidate from being saved:</h2>

      <ul>
      <% @candidate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    Name: <%= f.text_field :name %>

    Sex: <%= f.select(:sex, %w{ Male Female }) %>

    Age: <%= f.number_field :age %>

    Address: <%= f.text_area :address %> <br>

    Mobile: <%= f.number_field :mobile %>

    Email: <%= f.text_field :email %>

    PAN: <%= f.text_field :pan %>

    Marriage: <%= f.select(:married, %w{ Married Unmarried Divorcee }) %> <br>

    Spouse: <%= f.text_field :spouse_name %>

    Education: <%= f.text_field :education %>

    Occupation: <%= f.text_field :occupation %> <br>

    Income for 2012-13: <%= f.text_field :income_for_2012_2013 %> <br>

    <%= f.submit class: "btn btn-small btn-success active" %>

<% end %>
